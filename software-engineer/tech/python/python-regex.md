---
aliases:
  - Quantifier
  - Quantifiers
  - raw string
  - raw strings
  - raw-строка
  - raw-строки
  - re
  - re module
  - re.compile
  - re.findall
  - re.match
  - re.search
  - re.split
  - re.sub
  - Regex
  - RegExp
  - Regular expression
  - Regular expressions
  - Квантификатор
  - Квантификаторы
  - Класс символов
  - Классы символов
  - Метасимволы regex
  - Регулярки
  - Регулярное выражение
  - Регулярные выражения
  - Символьные классы
  - Символьный класс
---

## Регулярные выражения

### Предметно-ориентированный язык DSL

**[[DSL|DSL]]** (domain-specific language) — предметно-ориентированный язык, тип языков, которые специализируются на конкретной области применения. К данному типу относятся SQL и язык регулярных выражений.

### Синтаксис regex

Регулярное выражение (сокращённо **regex**) — это последовательность символов, определяющая шаблон поиска (**паттерн**). Главная сила регулярных выражений — в наборе метасимволов, которые позволяют гибко настраивать шаблон поиска.

Для доступа к методам работы с регулярными выражениями необходимо подключить модуль стандартной библиотеки `import re`. Регулярное выражение имеет вид `r"expression"`.

`pattern = r"([\w\.-]+)@([\w\.-]+)(\.[\w\.]+)"` — паттерн проверки формата e-mail.

### Методы модуля re

- `re.match(pattern, string)` — ищет по заданному шаблону в начале строки.
- `re.search(pattern, string)` — ищет по всей строке, но возвращает только первое найденное совпадение.
- `re.findall(pattern, string)` — возвращает список всех найденных совпадений.
- `re.split(pattern, string, [maxsplit=0])` — разделяет строку по заданному шаблону.
- `re.sub(pattern, repl, string, count=0, flags=0)` — замена всех вхождений шаблона на подстроку.
- `re.compile(pattern)` — собрать регулярное выражение в отдельный объект, который может быть использован для поиска. Это также избавляет от переписывания одного и того же выражения.

Демонстрация работы методов:

```python
import re
res = re.match(r'AV', 'AV Analytics Vidhya AV')
print( f'подстрока',res.group(),'найдена в символах',res.span() )
# подстрока AV найдена в символах (0, 2)
res = re.search(r'Analytics', 'AV Analytics Vidhya AV')
print( f'подстрока',res.group(),'найдена в символах с #',res.start(),'по',res.end() )
# подстрока Analytics найдена в символах с # 3 по 12
result = re.findall(r'AV', 'AV Analytics Vidhya AV')
print( result )
# ['AV', 'AV']
result = re.split(r'i', 'Analytics Vidhya')
print( result )
# ['Analyt', 'cs V', 'dhya'] # все возможные участки.
result = re.sub(r'India', 'the World', 'AV is largest Analytics community of India')
print( result )
# 'AV is largest Analytics community of the World'
pattern = re.compile('AV')
result = pattern.findall('AV Analytics Vidhya AV')
print( result )
result2 = pattern.findall('AV is largest analytics community of India')
print( result2 )
'''
['AV', 'AV']
['AV']
'''
```

### Группы внутри паттерна

Группы внутри паттерна соединены между собой **дизъюнктивно** (or). Каждая группа обособляется в скобки `(…)`.

Нумерация групп:

```python
import re
pattern = r"a(bc)(de)(f(g)h)i"
match = re.match(pattern, "abcdefghijklmnop")
if match:
    print(match.group())  # abcdefghi
    print(match.group(0)) # abcdefghi
    print(match.group(1)) # bc
    print(match.group(2)) # de
    print(match.groups()) # ('bc', 'de', 'fgh', 'g')
```

Группы внутри паттерна можно именовать. **Named groups** имеют формат `(?P<name>...)`. **Non-capturing groups** имеют формат `(?:...)`.

Именованные и незахватывающие группы:

```python
import re
pattern = r"(?P<first>abc)(?:def)(ghi)"
match = re.match(pattern, "abcdefghi")
if match:
    print(match.group("first")) # abc
    print(match.groups())       # ('abc', 'ghi')
```

Спецсимвол `|` внутри группы — логическая дизъюнкция.

Дизъюнкция внутри группы:

```python
import re
pattern = r"gr(a|e)y"
match = re.match(pattern, "gray")
if match:
    print ("Match 1")
match = re.match(pattern, "grey")
if match:
    print ("Match 2")
match = re.match(pattern, "griy")
if match:
     print ("Match 3")
'''
Match 1
Match 2
'''
```

Спецсимвол `\1` отсылает к группе согласно её номеру.

Обратная ссылка на группу:

```python
import re
pattern = r"(.+) \1"
match = re.match(pattern, "word word")
if match:
    print ("Match 1")
match = re.match(pattern, "?! ?!")
if match:
    print ("Match 2")
match = re.match(pattern, "abc cde")
if match:
    print ("Match 3")
'''
Match 1
Match 2
'''
```

### Символьные классы

Символьные классы в регулярных выражениях соответствуют сразу некоторому набору символов. Например, `\d` соответствует любой цифре от 0 до 9 включительно, `\w` — буквам и цифрам, а `\W` — всем символам, кроме букв и цифр. Шаблон, идентифицирующий буквы, цифры и пробел, выглядит так: `\w\s`.

- The class `[a-z]` matches any lowercase alphabetic character.
- The class `[G-P]` matches any uppercase character from G to P.
- The class `[0-9]` matches any digit.
- Multiple ranges can be included in one class. For example, `[A-Za-z]` matches a letter of any case.

Класс `[aeiou]` и диапазоны:

```python
# совпадение, если хотя бы 1 символ из строки входит в символьный класс [aeiou]
pattern = r"[aeiou]"
if re.search(pattern, "grey"):
    print("Match 1")
pattern = r"[A-Z][A-Z][0-9]"
if re.search(pattern, "LS8"):
    print("Match 1")
```

Символ `^` в качестве первого символа класса **инвертирует** символьный класс: подойдёт любая строка, кроме той, которая строго соответствует символьному классу.

Инвертирование класса `[^A-Z]`:

```python
import re
pattern = r"[^A-Z]"
if re.search(pattern, "this is all quiet"):
    print("Match 1")
if re.search(pattern, "AbCdEfG123"):
    print("Match 2")
if re.search(pattern, "THISISALLSHOUTING"):
    print("Match 3")
'''
Match 1
Match 2
'''
```

### Метасимволы regex

#### Спецсимволы regex

| Символ | Аналог | Описание |
|---|---|---|
| `()` | | подмаска, вложенное выражение |
| `[]` | | групповой символ |
| `{a,b}` | | количество вхождений от «a» до «b» |
| `\|` | | логическое «или», в случае с односимвольными альтернативами используйте `[]` |
| `\` | | экранирование спецсимвола |
| `.` | | любой символ, кроме перевода строки |
| `\d` | `[0-9]` | десятичная цифра |
| `\D` | `[^\d]` | любой символ, кроме десятичной цифры |
| `\f` | | конец (разрыв) страницы |
| `\n` | | перевод строки |
| `\pL` | | буква в кодировке UTF-8 при использовании модификатора u |
| `\r` | | возврат каретки |
| `\s` | `[ \t\v\r\n\f]` | пробельный символ |
| `\S` | `[^\s]` | любой символ, кроме пробельного |
| `\t` | | табуляция |
| `\w` | `[0-9a-z_]` | любая цифра, буква или знак подчеркивания |
| `\W` | `[^\w]` | любой символ, кроме цифры, буквы или знака подчеркивания |
| `\v` | | вертикальная табуляция |

#### Модификаторы

Символ `-` перед модификатором (за исключением `U`) создаёт его отрицание.

| Модификатор | Описание |
|---|---|
| `g` | глобальный поиск (обрабатываются все совпадения с шаблоном поиска) |
| `i` | игнорировать регистр |
| `m` | многострочный поиск: по умолчанию текст — одна строка, с модификатором — отдельные строки, поэтому `^` обозначает начало строки в тексте, а `$` — конец строки в тексте |
| `s` | текст воспринимается как одна строка, спецсимвол «точка» (`.`) будет включать и перевод строки |
| `u` | используется кодировка UTF-8 |
| `U` | инвертировать жадность |
| `x` | игнорировать все неэкранированные пробельные и перечисленные в классе символы |

#### Квантификаторы

| Оператор | Аналог | Пример | Описание |
|---|---|---|---|
| `?` | `{0,1}` | `a?` | одно или ноль вхождений «а» |
| `+` | `{1,}` | `a+` | одно или более вхождений «а» |
| `*` | `{0,}` | `a*` | ноль или более вхождений «а» |

#### Спецсимволы внутри символьного класса

| Символ | Пример | Описание |
|---|---|---|
| `^` | `[^da]` | отрицание, любой символ кроме «d» или «a» |
| `-` | `[a-z]` | интервал, любой символ от «a» до «z» |

#### Указатели позиции

| Символ | Пример | Соответствие | Описание |
|---|---|---|---|
| `^` | `^a` | **a**aa aaa | начало строки |
| `$` | `a$` | aaa aa**a** | конец строки |
| `\A` | `\Aa` | **a**aa aaaaaa aaa | начало текста |
| `\z` | `a\z` | aaa aaaaaa aa**a** | конец текста |
| `\b` | `a\b\ba` | aa**a** aa**aa**aa **a**aa | граница слова, утверждение: предыдущий символ словесный, а следующий — нет, либо наоборот |
| `\B` | `\Ba\B` | a**a**a a**a**a | отсутствие границы слова |
| `\G` | `\Ga` | **aaa** aaa | предыдущий успешный поиск, поиск остановился на 4-й позиции — там, где не нашлось `a` |

**Якоря** в регулярных выражениях указывают на начало или конец чего-либо, например строки или слова. Они представлены определёнными символами. К примеру, шаблон, соответствующий строке, начинающейся с цифры, должен иметь следующий вид: `^[0-9]+`.

Здесь символ `^` обозначает начало строки. Без него шаблон соответствовал бы любой строке, содержащей цифру.

#### POSIX

POSIX — это относительно новое дополнение семейства регулярных выражений. Идея, как и в случае с символьными классами, заключается в использовании сокращений, представляющих некоторую группу символов.
