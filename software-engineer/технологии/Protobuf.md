
### 🔹 **Protocol Buffers (Protobuf)** — от Google

#### 📌 Определение:
> **Protobuf** — это **язык независимый от языка программирования** для описания структур данных, который компилируется в эффективный бинарный код для сериализации/десериализации.

#### ✅ Пример `.proto` файла:
```protobuf
syntax = "proto3";

message User {
  int32 id = 1;
  string name = 2;
  string email = 3;
  bool is_active = 4;
}
```

→ Компилируете его через `protoc` → получаете классы на Java, Go, Python и т.д.

#### 💡 Как работает:
```python
# Python
user = User(id=123, name="Alice", email="alice@example.com", is_active=True)
serialized = user.SerializeToString()  # Бинарные данные (~30 байт)

# На другом конце:
deserialized = User()
deserialized.ParseFromString(serialized)
print(deserialized.name)  # Alice
```

#### ✅ Преимущества Protobuf:
| Плюс | Объяснение |
|------|------------|
| **Сверхкомпактный** | В 3–5x меньше, чем JSON |
| **Быстрая сериализация** | В 5–10x быстрее JSON |
| **Строгая типизация** | Компилятор проверяет поля, типы, обязательность |
| **Обратная совместимость** | Можно добавлять новые поля без ломки старых клиентов |
| **Поддержка всех языков** | Java, Go, Python, C#, Rust, JavaScript и др. |
| **Генерация кода** | Автоматически создаются классы — нет ручного парсинга |

#### ⚠️ Недостатки:
- Не человекочитаем (бинарный формат)
- Требует компиляции `.proto` → код
- Меньше гибкости, чем JSON (нет динамических полей)

> ✅ **Идеален для RPC (gRPC), событий в Kafka, внутренних API.**

---
[[Avro]]