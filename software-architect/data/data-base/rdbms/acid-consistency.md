---
aliases:
  - ACID
  - ACID consistency model
  - Atomicity
  - Consistency
  - data consistency model
  - data consistency models
  - Durability
  - Isolation
  - transaction
  - атомарность
  - долговечность
  - изолированность
  - согласованность
  - транзакция
---

## Требования к транзакционной системе ACID

**ACID** – это одна из **data consistency models**
### Atomicity (атомарность)

**Atomicity** (атомарность) — выражается в том, что транзакция должна быть **выполнена в целом или не выполнена вовсе**.

### Consistency (согласованность)

**Consistency** (согласованность) — гарантирует, что по мере выполнения транзакций, **данные переходят из одного согласованного состояния в другое**, то есть транзакция не может разрушить взаимной согласованности данных.

### Isolation (изолированность)

**[[transaction-isolation|Isolation]]** (изолированность) — локализация пользовательских процессов означает, что конкурирующие за доступ к БД транзакции физически обрабатываются последовательно, **изолированно друг от друга, но для пользователей это выглядит, как будто они выполняются параллельно**.

### Durability (долговечность)

**Durability** (долговечность) — **устойчивость к ошибкам** — если транзакция завершена успешно, то те изменения в данных, которые были ею произведены, не могут быть потеряны ни при каких обстоятельствах.

---

see [[base-consistency|BASE consistency model]]
